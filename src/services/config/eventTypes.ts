import { eventGroups } from '../eventGroups'

const moduleName = '@config'

export const CONFIG_REQUESTED = `${moduleName}/${eventGroups.REQUESTED}`
export const CONFIG_RECEIVED = `${moduleName}/${eventGroups.RECEIVED}`
export const CONFIG_ERRORED = `${moduleName}/${eventGroups.ERRORED}`


